<div id="<%= dom_id post %>" class="">
  <h1 class="text-4xl font-bold mb-5">
    <%= post.name %>
  </h1>

  <p class="my-5">
    <%= image_tag post.poster if post.poster.attached? %>
  </p>

  <p class="my-5">
    <%= post.content %>
  </p>
</div>
